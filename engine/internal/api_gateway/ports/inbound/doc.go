// Package inbound defines transport‑agnostic request/response contracts and handler
// interfaces used by the API Gateway. It models commands, results, request metadata,
// and a generic unary handler/middleware pattern so HTTP and gRPC can share logic.
package inbound
